# tests_python
Python tests for continuous integration

#to run all tests:
in test folder:
`export PYTHONPATH=.`
`nosetests`

#test suits
to use fast test suite - preferable durring development
`nosetests -a fast`

for slow tests
`nostests -a slow`

for load tests
`nosetests -a load`

